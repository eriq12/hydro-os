modules:
  - type: dnf
    install:
      packages:
        # zsh
        - zsh
        # sqlite for some autocomplete
        - sqlite
        # zoxide for z stuff
        - zoxide
  
  # install oh my zsh
  - type: script
    scripts:
      - installohmyzsh.sh