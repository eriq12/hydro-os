modules:
  - type: rpm-ostree
    repos:
      - rpmfusion
    install:
      - steam
      # for game mode and big picture mode
      - gamescope
      # for performance view
      - mangohud
  
  - type: default-flatpaks
    system:
      - net.davidotek.pupgui2